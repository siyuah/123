# Hermes session 80315662261f

Workspace: /home/siyuah/workspace
Model: gpt-5.4

## user

开工

## assistant

开工。

我看了下最近在做的事，最近 3 个会话是：
1. Diagnosing Localhost 8787 Access
2. 启动微信 Schema 服务
3. 把 72crm-pc 上传到 GitHub

你要我：
- 继续上次某个任务
- 开一个新任务
- 先帮你检查当前工作区项目

直接告诉我一句就行，比如：
- “继续查 8787”
- “启动这个项目”
- “先看看当前目录里有什么”

## user

I've uploaded 1 file(s): hermes_gpt54_starter.zip

## assistant

已开工，我已经把压缩包解开并看了下结构。

位置：
/home/siyuah/workspace/hermes_gpt54_starter

我确认到这是个 “Hermes-Agent + GPT-5.4 开工 Starter”，里面主要有：
- README.md
- AGENT_START_PROMPT.md
- control-plane/
  - package.json
  - tsconfig.json
  - config/model-routing.yaml
  - src/index.ts
  - src/config.ts
  - src/types.ts
  - src/utils/shell.ts
  - src/sandbox/worktree-manager.ts
  - src/sandbox/container-manager.ts
  - src/llm/budget-interceptor.ts
  - src/policies/assignment-policy.ts
  - src/policies/allowed-commands.ts

README 里写的目标是：
- 初始化 control-plane
- 实现 worktree/container 管理
- 加上模型路由和预算拦截
- 给 hermes-agent 一条能直接执行的启动指令

你下一句直接选一个，我就继续干：
- “先读 AGENT_START_PROMPT”
- “先把这个项目跑起来”
- “先审代码结构”
- “直接开始补实现”

## user

先读 AGENT_START_PROMPT

## assistant

我读完了，核心意思是：

目标
- 这是给 hermes-agent + GPT-5.4 的“开工指令”
- 当前只做第一周 P0：隔离骨架与最小闭环
- 本轮只聚焦 control-plane/，不要碰 board / issue / comment / daemon 主体逻辑

硬规则
- 优先保证：
  - Worktree 安全
  - Container 隔离
  - Budget 拦截
  - 模型路由可配置
- 不允许自由拼接危险 git 命令
- 只能通过 checkoutTaskBranch(taskId) 进入任务分支
- 不允许宽泛命令白名单，危险命令都要封装到 sandbox 层
- 每完成一个阶段，都要输出：
  - 新增/修改文件清单
  - 本地验证结果
  - 下一步建议

本轮优先要实现的文件
- control-plane/src/sandbox/worktree-manager.ts
- control-plane/src/sandbox/container-manager.ts
- control-plane/src/llm/budget-interceptor.ts
- control-plane/src/policies/assignment-policy.ts
- control-plane/config/model-routing.yaml

具体要求

1) worktree-manager
必须支持：
- createTaskWorktree(taskId, repoPath, baseCommit?)
- checkoutTaskBranch(taskId)
- cleanupTaskWorktree(taskId)
- pruneStaleWorktrees()

要求：
- 分支名只能是 task/<taskId>
- finally 里必须清理
- 允许 on-exit 兜底清理

2) container-manager
必须支持：
- 创建 task 级容器
- 设置 CPU / Memory / Pids 限制
- 支持 setup timeout 和 boot timeout
- 支持优雅停止：SIGTERM -> 等待 -> SIGKILL

3) budget-interceptor
必须支持：
- 软限制 80%
- 硬限制 100%
- 流式 token 累计
- 达到阈值后阻止后续模型调用

4) assignment-policy
必须支持任务类型：
- feature
- bugfix
- refactor
- research
- triage

模型角色要求：
- 主脑：GPT-5.4
- 长时执行：GLM-5.1
- coder：Qwen3-Coder-Next / Plus
- 根因分析：DeepSeek-Reasoner
- long-context / bulk-worker：Qwen3.6 Plus / Flash

它要求的开始顺序
1. 初始化 control-plane 的 package.json 和 tsconfig
2. 创建 src/sandbox/、src/llm/、src/policies/
3. 优先完成 worktree-manager.ts
4. 完成后先停下，输出文件树和实现摘要，等 review

补一句：
你这个 starter 里其实这些目录和文件骨架已经有了，所以“初始化”这一步很可能已经做过，下一步更像是“审现有骨架 + 补 worktree-manager 实现”。

如果你愿意，我下一步直接：
- 读取并审查 worktree-manager.ts
- 顺手看它是否已经满足这份开工指令

你只要回一句：
“继续读 worktree-manager”

## user

继续读 worktree-manager

## assistant

我已经把 worktree-manager 读完了，顺手也看了它依赖的 shell 封装。

当前实现做了什么

文件：
/home/siyuah/workspace/hermes_gpt54_starter/control-plane/src/sandbox/worktree-manager.ts

已有能力：
- getBranchName(taskId)
  - 强制分支名为 task/<taskId>
- getWorktreePath(taskId)
  - worktree 路径为 workspaceRoot/<taskId>
- createTaskWorktree(taskId, baseRef = "HEAD")
  - 创建 workspaceRoot
  - 先删残留目录
  - 执行：
    git worktree add -B task/<taskId> <worktreePath> <baseRef>
- checkoutTaskBranch(taskId)
  - 只允许 checkout 到 task/<taskId>
- cleanupTaskWorktree(taskId)
  - 先 git worktree remove --force
  - 失败时兜底 rm -rf 目录
  - 然后 git worktree prune
- pruneStaleWorktrees()
  - 执行 git worktree prune
- assertTaskId(taskId)
  - 只允许 [A-Za-z0-9._-]
  - 能防路径穿越和分支注入

依赖的 shell 封装
文件：
/home/siyuah/workspace/hermes_gpt54_starter/control-plane/src/utils/shell.ts

特点：
- 用 spawn(command, args) 执行，不是拼接 shell 字符串
- 支持 cwd / env / timeoutMs
- 超时会：
  - 先 SIGTERM
  - 5 秒后 SIGKILL
- 返回 stdout / stderr / code

和开工指令对比，哪些已经满足

已满足：
- 只能用 task/<taskId> 分支命名
- 不暴露任意 checkout
- worktree 生命周期集中在 sandbox 层
- 有 cleanupTaskWorktree()
- 有 pruneStaleWorktrees()
- taskId 做了安全校验
- 命令执行走参数数组，不是危险字符串拼接

还没完全满足/值得注意的点

1. createTaskWorktree 的签名和指令不完全一致
指令要求：
- createTaskWorktree(taskId, repoPath, baseCommit?)

现在是：
- createTaskWorktree(taskId, baseRef = "HEAD")

因为 repoPath 已经在 constructor 里了，所以功能上不算缺，但“接口形式”跟文档不一致。

2. 还没有看到 finally 中必须清理 的使用范式
这个文件只提供了 cleanupTaskWorktree()，但没有：
- 一个高层包装器负责 try/finally
- 或示例调用保证 finally 清理

也就是说：
- “有清理能力”
- 但“还没保证调用方一定在 finally 清理”

3. 还没有 on-exit 兜底清理
指令要求允许 on-exit fallback cleanup。
当前文件里还没看到：
- process.on("exit", ...)
- SIGINT / SIGTERM hook
- 未清理 worktree 注册表

4. create 前直接 rm worktreePath 有点粗暴
现在会先删除目录，再 git worktree add。
风险是：
- 如果 git 仍记录这个 worktree，单删目录可能造成残留 worktree metadata
- 更稳的做法通常是先尝试 git worktree remove/prune，再删目录

5. checkoutTaskBranch 的价值目前偏弱
因为 createTaskWorktree 里已经用：
- git worktree add -B branch ...
通常分支已经切好了。
所以这个方法更像“额外防守接口”，不是核心创建路径。

一句话评价

这是一个“方向对、骨架可用”的第一版：
- 安全边界意识是对的
- 分支命名和命令封装也对
- 但离 AGENT_START_PROMPT 要求的“最小闭环”还差两块关键补强：
  - finally 清理闭环
  - on-exit 兜底清理

如果你要，我下一步可以直接继续：
- 读 container-manager.ts
或者
- 直接给你列一个 worktree-manager 的改造清单

你回一句：
- “继续读 container-manager”
或
- “给我 worktree 改造清单”

## user

继续读 container-manager

## assistant

我已经读完 container-manager，也顺手看了 types。

文件
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/src/sandbox/container-manager.ts
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/src/types.ts

container-manager 当前实现

1. 配置结构
TaskContainerConfig 支持：
- taskId
- image
- worktreePath
- memory
- cpus
- pidsLimit
- setupTimeoutSeconds
- bootTimeoutSeconds
- networkDisabled?
- environment?
- command?

2. 容器命名
- getContainerName(taskId)
- 命名规则：df-task-<taskId>
- taskId 也做了正则校验，避免注入

3. 创建容器
createTaskContainer(config)
现在会执行大致等价于：

docker run -d \
  --name df-task-<taskId> \
  --memory <memory> \
  --cpus <cpus> \
  --pids-limit <pidsLimit> \
  -v <worktreePath>:/workspace \
  -w /workspace \
  [--network none] \
  [-e KEY=VALUE ...] \
  <image> \
  <command... or sleep infinity>

已覆盖：
- task 级容器
- CPU / Memory / Pids 限制
- 可挂载任务 worktree
- 可选禁网
- 可传环境变量
- 默认命令是 sleep infinity

4. 容器内执行
exec(name, command, timeoutMs)
- 走 docker exec
- 支持超时

5. 优雅停止
stopContainer(name, gracefulSeconds = 10)
逻辑：
- 先 docker stop -t <seconds> <name>
- 如果失败，再 docker kill <name>

这基本符合：
- SIGTERM -> 等待 -> SIGKILL

6. 删除容器
removeContainer(name)
- docker rm -f <name>
- 失败吞掉

跟 AGENT_START_PROMPT 对比

已满足的部分
- 创建 task 级容器
- 设置 CPU / Memory / Pids 限制
- 支持优雅停止：SIGTERM -> 等待 -> SIGKILL
- 命令是参数数组传递，不是 shell 拼接
- taskId 有安全校验

没完全满足/明显缺口

1. setup timeout / boot timeout 只是“字段存在”，但没有真正生效
这是当前最大缺口。

虽然 config 里有：
- setupTimeoutSeconds
- bootTimeoutSeconds

但 createTaskContainer 里完全没用这两个字段：
- docker run 的 timeout 仍写死为 60_000
- 没有 setup 阶段
- 没有 boot readiness 检查
- 没有 wait-until-ready 逻辑

所以目前只能说：
- “配置声明了”
- 但“功能没有实现”

2. 没有容器启动后的健康/可用性确认
Prompt 里说支持 boot timeout，通常意味着至少要有一种：
- 等待容器 running
- 检查 docker inspect State.Running
- 执行探测命令
- 健康检查 health status

当前没有这类逻辑。

3. create 前没有处理同名残留容器
现在直接 docker run --name df-task-<taskId>
如果上次残留同名容器，会直接失败。
更稳的做法通常是：
- 创建前先 rm -f 同名容器
或
- 检测并复用/报错

4. 缺少 finally / on-exit 级别的统一清理闭环
和 worktree-manager 一样，这里有 removeContainer()，但没看到：
- 高层 try/finally 使用范式
- 进程退出时批量清理机制

5. 还没有更强的隔离参数
Prompt 主要要求是 CPU/Memory/Pids 和隔离，当前已做到基础版。
但如果追求更稳，后续还可以考虑：
- --read-only
- --cap-drop ALL
- --security-opt no-new-privileges
- 用户映射 / 非 root
- 临时目录 tmpfs
不过这些属于加强项，不是你这份 prompt 里的硬性最低要求。

一句话评价

container-manager 是“可跑的基础版”，但还不是“满足指令的完成版”。

最关键判断：
- 资源限制：有
- 优雅停止：有
- setup/boot timeout：名义上有，实际上没实现

如果按开工指令严格打分，我会说：
- worktree-manager：大体 70%
- container-manager：大体 60%
因为 container-manager 的 timeout 需求还没真正落地

我建议下一步有两个方向：
1. 继续读 budget-interceptor
2. 我直接给你列一个 container-manager 的补强清单

你回一句：
- “继续读 budget-interceptor”
或
- “给我 container 改造清单”

## user

继续读 budget-interceptor

## assistant

我读完了 budget-interceptor。

文件
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/src/llm/budget-interceptor.ts

当前实现内容

它定义了 3 组类型：

1. UsageSnapshot
- promptTokens
- completionTokens
- estimatedUsd

2. BudgetConfig
- budgetLimitUsd
- softLimitRatio
- hardLimitRatio

3. BudgetDecision
- allowed
- softLimited
- hardLimited
- reason?

核心类
- BudgetInterceptor(config)

已有两个方法：

1) preflight(currentSpendUsd)
作用：
- 在模型调用前，根据当前累计花费判断能不能继续

逻辑：
- softLimit = budgetLimitUsd * softLimitRatio
- hardLimit = budgetLimitUsd * hardLimitRatio

判断结果：
- currentSpendUsd >= hardLimit
  - allowed: false
  - softLimited: true
  - hardLimited: true
  - reason: Budget hard limit reached
- currentSpendUsd >= softLimit
  - allowed: true
  - softLimited: true
  - hardLimited: false
  - reason: Budget soft limit reached
- 否则
  - allowed: true
  - softLimited: false
  - hardLimited: false

2) onStreamProgress(currentSpendUsd)
作用：
- 作为流式过程中的检查入口
- 现在只是直接调用 preflight(currentSpendUsd)

跟开工指令对比

已满足
- 软限制（80%）机制：支持
  - 通过 softLimitRatio 配置
- 硬限制（100%）机制：支持
  - 通过 hardLimitRatio 配置
- 达到硬阈值后阻止后续模型调用：支持
  - preflight 返回 allowed: false
- 有“流式累计检查入口”：勉强算有
  - onStreamProgress() 存在

没完全满足/明显缺口

1. “流式 token 累计”还没真正实现
这是最大缺口。

现在 onStreamProgress 接收的是：
- currentSpendUsd

也就是说：
- 它并不自己累计 token
- 不接收 chunk token 增量
- 不维护内部状态
- 不把 promptTokens / completionTokens 转成成本

而 prompt 明确要的是：
- 流式 token 累计
- 达阈值后阻止后续模型调用

当前实现更像：
- “外部已经算好了累计花费，我这里只负责判定”

所以它是一个判定器，不是完整的 interceptor。

2. UsageSnapshot 定义了，但没有真正用起来
文件里有：
- promptTokens
- completionTokens
- estimatedUsd

但实际逻辑完全没使用 UsageSnapshot。

3. 没有“后续调用阻断状态”的内部管理
Prompt 的语义更像：
- 一旦达到硬限制，后续调用都应该被拒绝

但当前类没有保存任何状态，例如：
- totalPromptTokens
- totalCompletionTokens
- totalSpendUsd
- blocked

所以：
- 每次都是纯函数式判断
- 不是“真正的拦截器状态机”

4. 软限制没有降级策略，只是打标记
现在 soft limit reached 时：
- allowed 仍然是 true

这不算错，但通常还需要上层配合：
- 降级到更便宜模型
- 降低 max_tokens
- 禁止并发子任务
- 只允许关键调用

当前文件里没有这些动作，只返回标志。

一句话评价

budget-interceptor 现在是“预算阈值判定骨架”，不是“完整预算拦截器”。

如果按 prompt 严格看：
- 软限制：有
- 硬限制：有
- 流式 token 累计：基本还没实现
- 超阈值阻止后续调用：只有静态判定，缺少状态化拦截闭环

所以我会给它一个：
- 完成度约 50%～60%

目前这三个文件的整体判断

- worktree-manager：方向对，缺 finally/on-exit 闭环
- container-manager：资源限制和停止逻辑有了，但 setup/boot timeout 未真正实现
- budget-interceptor：阈值判断有了，但流式累计和状态化拦截还没落地

如果你愿意，下一步最自然的是：
- 继续读 assignment-policy

你回一句：
- “继续读 assignment-policy”

## user

继续读 assignment-policy

## assistant

我读完 assignment-policy 了。

文件
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/src/policies/assignment-policy.ts

它在做什么

核心函数：
- assignModels(ctx: TaskContext): AssignmentResult

输入：
- TaskContext，来自 src/types.ts
- 包含 taskType / risk / retryCount / filesChangedEstimate / modulesChangedEstimate / touchesCoreLogic / protectedPathTouched 等上下文

输出：
- AssignmentResult
- 会返回一组角色分配，比如：
  - lead
  - executor
  - coder
  - reviewer
  - secondReviewer
  - rootCause
  - analyst
  - worker
  - bulkWorker
  - finalDecider
  - handoffToHuman
  - reason[]

当前路由逻辑

1. 先判断是否人工接管
条件：
- retryCount >= 4
- protectedPathTouched
- scopeExpandedBeyondTask
- securityOrPaymentLogicChanged

如果命中：
- lead: gpt-5.4
- handoffToHuman: true
- reason: 触发人工接管规则

2. 决定 coder 档位
useCoderPlus 条件：
- filesChangedEstimate > 12
- modulesChangedEstimate > 3
- touchesCoreLogic

命中则：
- coder 用 qwen3-coder-plus
否则：
- coder 用 qwen3-coder-next

3. 决定是否用根因分析模型
useReasoner 条件：
- retryCount >= 2
- testFailuresNotObvious
- risk === high

命中则倾向：
- rootCause: deepseek-reasoner
- 或 secondReviewer: deepseek-reasoner

4. feature 路由
高风险 feature：
- lead: gpt-5.4
- executor: glm-5.1
- coder: qwen3-coder-plus / next
- reviewer: gpt-5.4
- secondReviewer: deepseek-reasoner

普通 feature：
- lead: gpt-5.4
- executor: glm-5.1
- coder: qwen3-coder-plus / next
- bulkWorker: qwen3.6-flash

5. bugfix 路由
需要 reasoner：
- lead: gpt-5.4
- executor: glm-5.1
- coder: qwen3-coder-plus / next
- rootCause: deepseek-reasoner
- finalDecider: gpt-5.4

普通 bugfix：
- lead: gpt-5.4
- executor: glm-5.1
- coder: qwen3-coder-next
- bulkWorker: qwen3.6-flash

6. refactor 路由
- lead: gpt-5.4
- coder: qwen3-coder-plus / next
- reviewer: gpt-5.4
- secondReviewer: deepseek-reasoner（仅在 useReasoner 时）

7. research 路由
- analyst: qwen3.6-plus
- reviewer: gpt-5.4

8. triage 路由（默认落到最后）
- worker: qwen3.6-flash
- analyst: deepseek-chat

跟开工指令对比

明确满足的部分
任务类型支持：
- feature
- bugfix
- refactor
- research
- triage
都覆盖了。

模型角色要求对照：

1) 主脑：GPT-5.4
- 满足
- 多处作为 lead / reviewer / finalDecider

2) 长时执行：GLM-5.1
- 满足
- 在 feature / bugfix 里作为 executor

3) coder：Qwen3-Coder-Next / Plus
- 满足
- 通过复杂度条件二选一

4) 根因分析：DeepSeek-Reasoner
- 基本满足
- 在 bugfix / 高风险 feature / refactor 中使用

5) long-context / bulk-worker：Qwen3.6 Plus / Flash
- 基本满足
- research 用 qwen3.6-plus
- feature/bugfix/triage 用 qwen3.6-flash

存在的问题/偏差

1. triage 用了 deepseek-chat，不在 prompt 指定列表里
这是最明显的偏差。

Prompt 指定的关键角色是：
- GPT-5.4
- GLM-5.1
- Qwen3-Coder-Next / Plus
- DeepSeek-Reasoner
- Qwen3.6 Plus / Flash

但 triage 分支用了：
- analyst: deepseek-chat

这不在要求名单里。
如果严格对齐 prompt，这里更可能应该用：
- analyst: qwen3.6-plus
或
- analyst/reviewer: gpt-5.4
或
- rootCause/analyst: deepseek-reasoner

2. research 分支没有明确“lead”
这不一定错，但从整体一致性看：
- feature/bugfix/refactor 都有 lead
- research 没有

如果 control-plane 上层想统一依赖 lead 字段，可能会不一致。

3. triage 分支也没有 lead
同上，也是接口一致性问题。

4. model-routing.yaml 还没联动
现在 assignment-policy 里的模型名是硬编码字符串：
- "gpt-5.4"
- "glm-5.1"
- "qwen3-coder-plus"
- "deepseek-reasoner"

而开工指令强调：
- 模型路由可配置
- 模型角色集中配置

所以从架构角度看，assignment-policy 目前是：
- “策略有了”
- 但“没有真正从 config/model-routing.yaml 读取”

这点很关键，因为一旦模型名变了，现在要改代码。

一句话评价

assignment-policy 是目前四个文件里“完成度最高”的一个。
它已经把：
- 任务类型
- 风险升级
- 人工接管
- 各模型角色分工
这些骨架基本搭起来了。

但如果严格按 prompt，要补两件事：
- 去掉/替换 deepseek-chat 这种未在目标清单里的模型
- 把模型名从硬编码改成从 model-routing.yaml 读取

我给它的完成度大概会是：
- 75%～85%

现在四个文件读完后的总评

1. worktree-manager
- 安全边界对
- 缺 finally / on-exit 清理闭环

2. container-manager
- 基础容器控制对
- setup/boot timeout 还没真正实现

3. budget-interceptor
- 预算阈值判断有
- 流式 token 累计和状态化拦截没真正实现

4. assignment-policy
- 策略最完整
- 但有硬编码模型名，且 triage 用了 prompt 外模型

如果你要，我下一步最合适的是二选一：
- 继续读 model-routing.yaml
- 或我直接给你出一份“当前骨架完成度审计清单”

你回一句：
- “继续读 model-routing.yaml”
或
- “给我完成度审计清单”

## user

继续读 model-routing.yaml

## assistant

我读完 model-routing.yaml 了。

文件
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/config/model-routing.yaml

当前内容结构

1. defaults
默认角色映射：
- lead: gpt-5.4
- executor: glm-5.1
- coder: qwen3-coder-next
- reviewer: gpt-5.4
- root_cause: deepseek-reasoner
- analyst: deepseek-chat
- long_context_worker: qwen3.6-plus
- bulk_worker: qwen3.6-flash

2. fallbacks
模型回退链：
- gpt-5.4 -> glm-5.1
- glm-5.1 -> qwen3-coder-plus
- qwen3-coder-next -> qwen3-coder-plus
- deepseek-reasoner -> gpt-5.4
- qwen3.6-flash -> qwen3.6-plus

3. rules
现在只写了 3 条规则：

- low-risk-feature
  - feature + low
  - lead: gpt-5.4
  - executor: glm-5.1
  - coder: qwen3-coder-next
  - bulk_worker: qwen3.6-flash

- high-risk-feature
  - feature + high
  - lead: gpt-5.4
  - executor: glm-5.1
  - coder: qwen3-coder-plus
  - reviewer: gpt-5.4
  - second_reviewer: deepseek-reasoner

- repeated-failure-bugfix
  - bugfix + retry_count_gte: 2
  - lead: gpt-5.4
  - executor: glm-5.1
  - coder: qwen3-coder-next
  - root_cause: deepseek-reasoner
  - final_decider: gpt-5.4

跟开工指令对比

满足的部分
- 主脑：gpt-5.4
- 长时执行：glm-5.1
- coder：qwen3-coder-next / plus
- 根因分析：deepseek-reasoner
- long-context / bulk-worker：qwen3.6-plus / flash

这些角色在 yaml 里都出现了。

明显问题

1. analyst 仍然是 deepseek-chat
这和刚才 assignment-policy 里的问题一致。
Prompt 指定的是：
- 根因分析：DeepSeek-Reasoner
- long-context / bulk-worker：Qwen3.6 Plus / Flash

但这里 defaults 里写的是：
- analyst: deepseek-chat

这不太对齐 prompt。

2. rules 覆盖不全
Prompt 要求 assignment-policy 至少支持：
- feature
- bugfix
- refactor
- research
- triage

但 yaml 里只覆盖了：
- feature（low/high 两种）
- bugfix（retry>=2 的一种）

缺：
- 普通 bugfix
- refactor
- research
- triage

3. 和 assignment-policy 还没真正打通
虽然 yaml 已经存在，但 assignment-policy.ts 目前还是硬编码模型名。
也就是说现在 yaml 更像“配置草案”，不是“实际生效配置”。

4. 字段命名风格有两套
在 TypeScript 的 AssignmentResult 里常见的是：
- secondReviewer
- rootCause
- finalDecider
- bulkWorker

但 yaml 里写的是：
- second_reviewer
- root_cause
- final_decider
- bulk_worker
- long_context_worker

这本身不是错，但需要一个明确的 config 解析层去做 snake_case -> camelCase 映射。
如果没有，就会出现：
- 配置写了
- 代码取不到

我对整体状态的判断

现在 starter 的“模型路由”是有两层骨架：
- 一层是 assignment-policy.ts 里的硬编码策略
- 一层是 model-routing.yaml 里的配置草案

但两层还没真正闭环：
- yaml 不完整
- policy 也没从 yaml 读取

所以“模型路由可配置”这项，严格说现在还没有完成，只是开始了。

当前 5 个目标文件读完后的总结

1. worktree-manager.ts
- 骨架正确
- 缺 finally 清理与 on-exit 兜底

2. container-manager.ts
- 资源限制和优雅停止已具备
- setup/boot timeout 尚未真正实现

3. budget-interceptor.ts
- 软硬阈值判定已有
- 流式 token 累计和状态化阻断未真正实现

4. assignment-policy.ts
- 任务分类与角色分工最完整
- 仍有硬编码，且用了 prompt 外模型 deepseek-chat

5. model-routing.yaml
- 有 defaults / fallbacks / rules 骨架
- 规则覆盖不足
- 与 TS 策略层未打通
- 也保留了 deepseek-chat 这个偏差

如果你愿意，下一步我建议直接进入“收口阶段”，我可以给你一份：
- 当前 starter 的完成度审计清单
或者
- 直接开始改代码，把这些缺口补上

你回一句：
- “给我完成度审计清单”
或
- “直接开始补代码”

## user

直接开始补代码

## assistant

已开始补代码，并且已经把这一轮补完、跑通验证了。

新增/修改文件清单

核心实现
- control-plane/src/utils/shell.ts
- control-plane/src/sandbox/worktree-manager.ts
- control-plane/src/sandbox/container-manager.ts
- control-plane/src/llm/budget-interceptor.ts
- control-plane/src/policies/assignment-policy.ts
- control-plane/config/model-routing.yaml

测试与脚本
- control-plane/package.json
- control-plane/tests/control-plane.test.ts

安装/构建产物
- control-plane/package-lock.json
- control-plane/node_modules/...
- control-plane/dist/...

本轮实现摘要

1) worktree-manager
已补上：
- 支持 createTaskWorktree(taskId, repoPath?, baseCommit?)
- 仍强制分支名只能是 task/<taskId>
- 创建前先 git worktree prune
- 支持注入 command runner，方便测试
- cleanupTaskWorktree 在 finally 中保证：
  - 从 tracked 集合移除
  - 再做 prune
- 增加进程退出兜底清理：
  - beforeExit
  - SIGINT
  - SIGTERM
- 增加 dispose() 释放监听器

2) container-manager
已补上：
- 支持注入 command runner，方便测试
- 创建前先 rm -f 同名容器，避免残留冲突
- setupTimeoutSeconds 真正用于 docker run timeout
- bootTimeoutSeconds 真正用于启动后轮询 docker inspect
- 容器未进入 running 状态会超时报错
- 优雅停止仍保持：
  - docker stop -t N
  - 失败则 docker kill

3) budget-interceptor
已补上：
- 内部累计 usage 状态
- onStreamProgress(delta) 现在会累加：
  - promptTokens
  - completionTokens
  - estimatedUsd
- preflight() 默认检查当前累计支出
- 达到 hard limit 后会阻止后续调用
- 新增：
  - getUsage()
  - reset()

4) assignment-policy
已补上：
- 支持 assignModels(ctx, routingConfig?)
- 支持从 model-routing.yaml 读取默认角色和规则覆盖
- 做了 snake_case -> camelCase 角色映射：
  - root_cause -> rootCause
  - second_reviewer -> secondReviewer
  - bulk_worker -> bulkWorker
  - final_decider -> finalDecider
- research / triage 不再使用 deepseek-chat
- research / triage 现在也有 lead，可与其它任务类型保持一致

5) model-routing.yaml
已补上/修正：
- analyst 从 deepseek-chat 改为 qwen3.6-plus
- 增加 defaults:
  - worker
  - final_decider
- 增加 rules:
  - standard-bugfix
  - refactor
  - research
  - triage

本地验证结果

已执行：
- npm install
- npm test
- npm run build
- npm run typecheck

结果：
- 测试：4/4 通过
- build：通过
- typecheck：通过

测试覆盖了：
- worktree 只能走 task/<taskId> 分支 + sandbox 命令路径
- container 的资源限制、setup timeout、boot readiness 轮询
- budget 的流式累计与 hard limit 阻断
- assignment policy 读取 routing config，且 research/triage 不再落到 deepseek-chat

当前文件树补充

你现在多了这些关键目录/文件：
- control-plane/tests/control-plane.test.ts
- control-plane/dist/
- control-plane/node_modules/
- control-plane/package-lock.json

下一步建议

建议按 prompt 的节奏，先停在这里做 review。
下一步最值得继续的是二选一：

1. 继续补“真正闭环”
- 增加高层 orchestration，显式用 try/finally 串起：
  - createTaskWorktree
  - createTaskContainer
  - cleanup
这样就不只是“能力具备”，而是“调用路径保证 finally 清理”。

2. 补配置驱动更彻底
- 让 assignment-policy 不只是“读取 defaults/rule 名”，
  而是按 yaml 的 when 条件统一匹配，减少 TS 里硬编码分支。

如果你要，我下一步可以直接继续：
- “做 worktree/container 的最小 orchestration 闭环”
或
- “继续把 assignment-policy 完全配置化”

## user

做 worktree/container 的最小 orchestration 闭环

## assistant

已补上 worktree/container 的最小 orchestration 闭环。

新增/修改文件清单
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/src/orchestration/task-runtime-orchestrator.ts
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/tests/control-plane.test.ts

本轮实现摘要

1. 新增最小 orchestration 模块
文件：
- src/orchestration/task-runtime-orchestrator.ts

新增：
- TaskRuntimeOrchestrator
- runTaskSandbox(options)

行为：
- 先 createTaskWorktree(taskId, repoPath, baseCommit)
- 再 createTaskContainer(...)
- 再执行用户传入的 run({ worktreePath, containerName })
- 最后在 finally 里统一清理：
  - stopContainer(containerName)
  - removeContainer(containerName)
  - cleanupTaskWorktree(taskId)

这样现在已经不是“只有清理能力”，而是有了一个明确的最小调用闭环，能保证：
- 成功路径会清理
- 失败路径也会清理

2. 按 TDD 补了两条 orchestration 测试
追加到：
- tests/control-plane.test.ts

新增覆盖：
- 成功执行后仍会 stop/remove container + cleanup worktree
- run 阶段抛错时，也会走同样的 finally 清理路径

本地验证结果

已执行：
- npm test -- --test-name-pattern=TaskRuntimeOrchestrator
- npm test
- npm run build
- npm run typecheck

结果：
- 测试 6/6 通过
- build 通过
- typecheck 通过

这轮完成后的意义

你原来缺的是“高层保证 finally 清理的调用路径”。
现在已经有了一个最小闭环入口：

- worktree 生命周期由 orchestrator 串起来
- container 生命周期由 orchestrator 串起来
- run 阶段无论成功还是失败，都会回收资源

下一步建议

如果继续往前推进，我建议二选一：

1. 把它接到 index.ts 做一个最小 demo 入口
比如：
- 读取 config
- 创建 orchestrator
- 跑一个 sample task
这样就从“模块存在”变成“可以直接演示闭环”。

2. 补 runtime guardrail
例如：
- orchestrator 内接 BudgetInterceptor
- 在 run 前/过程中做 budget preflight
这样 P0 的隔离闭环会更完整。

如果你要，我下一步直接继续：
- “把 orchestrator 接到 index.ts”
或
- “把 budget 也接进 orchestration”

## user

把 budget 也接进 orchestration

## assistant

已把 budget 接进 orchestration，而且已经按 TDD 跑通。

新增/修改文件清单
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/src/orchestration/task-runtime-orchestrator.ts
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/tests/control-plane.test.ts

本轮实现摘要

1. orchestrator 现在支持注入 BudgetInterceptor
TaskRuntimeOrchestrator 的依赖新增：
- budgetInterceptor?: BudgetInterceptor

2. 启动前做 budget preflight
在 runTaskSandbox() 最开始：
- 先创建 budget handle
- 立即执行 preflight()
- 如果已经 hard limit，则直接拒绝启动
- 此时不会创建 worktree，也不会创建 container

3. 运行时把 budget 能力暴露给 task
run(...) 回调现在拿到：
- worktreePath
- containerName
- budget

其中 budget 提供：
- preflight()
- recordUsage(delta)
- getUsage()
- assertWithinLimit()

典型用法：
- 模型流式返回 chunk 时调用 recordUsage(...)
- 每次关键模型调用前调用 assertWithinLimit()
- 超预算时直接抛错，中断任务

4. 预算超限时仍然走 finally 清理
即使 run 阶段因为 budget.assertWithinLimit() 抛错：
- 仍会 stopContainer
- removeContainer
- cleanupTaskWorktree

5. 无 budgetInterceptor 时仍可运行
我加了一个 unlimited budget handle：
- 默认永远 allowed
- 不影响现有调用路径
- 所以这个改动是向后兼容的

新增测试

我补了两条新测试：

1) 启动前已经达到 hard limit
验证：
- runTaskSandbox() 直接拒绝
- 不创建 worktree
- 不创建 container

2) 运行中记录 usage 后达到 hard limit
验证：
- run() 内能访问 budget handle
- recordUsage() 会累计金额
- assertWithinLimit() 会在 hard limit 时抛错
- 抛错后仍然清理 container 和 worktree

本地验证结果

已执行：
- npm test -- --test-name-pattern=budget
- npm test
- npm run build
- npm run typecheck

结果：
- tests: 8/8 通过
- build: 通过
- typecheck: 通过

当前闭环状态

现在 orchestration 的最小闭环已经是：

1. budget preflight
2. create worktree
3. create container
4. run task with budget handle
5. finally:
   - stop container
   - remove container
   - cleanup worktree

也就是说，P0 里这几块已经开始串起来了：
- Worktree 安全
- Container 生命周期
- Budget 拦截
- finally 清理闭环

下一步建议

最自然的下一步是：
- 把 orchestrator 接到 src/index.ts，做一个最小 demo / sample runtime

这样你就不只是“模块都写好了”，而是已经有一个能直接启动、能展示预算与隔离闭环的入口。

如果你要，我下一步直接：
- 把 orchestrator 接到 index.ts
并输出一个最小可运行 demo。

## user

orchestrator 接到 index.ts，并输出一个最小可运行 demo

## assistant

已接上，而且我已经把最小 demo 跑通了。

新增/修改文件清单
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/src/demo/minimal-demo.ts
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/src/index.ts
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/package.json
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/tests/control-plane.test.ts

本轮实现摘要

1. 新增最小 demo runner
文件：
- src/demo/minimal-demo.ts

提供：
- runMinimalDemo(config?)

它会做一个“安全演示版”的 orchestration：
- 构造 demo task
- 用 assignment-policy 做模型分配
- 创建 BudgetInterceptor
- 用 fake WorktreeManager / fake ContainerManager 跑 TaskRuntimeOrchestrator
- 在 run 阶段模拟一次 budget usage 增量
- 返回完整 demo summary

注意：
- 这是 safe demo
- 不会真的起 docker
- 不会真的操作 git worktree
- 但会完整展示 orchestration 的调用顺序与预算流转

2. index.ts 已接入 demo 模式
现在：
- 默认执行：仍加载 config 并打印 boot 信息
- 带 --demo 执行：运行最小 orchestration demo，并打印 JSON 结果

3. package.json 新增 demo 脚本
新增：
- npm run demo

等价于：
- tsx src/index.ts --demo

新增测试
我补了 1 条测试：
- runMinimalDemo returns a safe demo summary using the orchestrator

它验证：
- demoMode 为 true
- task/assignment/runtme 字段正确
- budgetAfterRun 为 0.42
- steps 顺序正确
- cleanup 仍在最后执行

本地验证结果

已执行：
- npm test -- --test-name-pattern=runMinimalDemo
- npm test
- npm run build
- npm run typecheck
- npm run demo

结果：
- tests: 9/9 通过
- build: 通过
- typecheck: 通过
- demo: 成功输出 JSON

你现在可以直接这样跑

进入目录：
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane

执行：
- npm run demo

我实际跑出来的 demo 输出核心内容是：

- taskId: demo-task
- assignment.lead: gpt-5.4
- assignment.executor: glm-5.1
- assignment.coder: qwen3-coder-next
- runtime.worktreePath: /demo/worktrees/demo-task
- runtime.containerName: demo-container-demo-task
- runtime.budgetAfterRun.estimatedUsd: 0.42

步骤序列：
- demo-worktree:create:demo-task
- demo-container:create:demo-task:/demo/worktrees/demo-task
- demo-run:budget-before:0
- demo-run:budget-after:0.42
- demo-container:stop:demo-container-demo-task:10
- demo-container:remove:demo-container-demo-task
- demo-worktree:cleanup:demo-task

这意味着什么

现在这个 starter 已经不只是“模块骨架”了，而是已经有一个最小可运行入口，能直接演示：
- 模型分配
- budget 注入
- worktree/container orchestration
- finally 清理闭环

下一步如果继续推进，最值钱的是二选一：

1. 把 demo fake manager 替换成真实 manager 的 CLI 入口
- 接 repoPath / taskId / image 参数
- 变成真实 P0 runner

2. 给 index.ts 增加命令行参数解析
例如：
- --demo
- --task-id
- --repo-path
- --base-commit
- --image

如果你愿意，我下一步直接把它做成“真实可调用的 CLI runner”。

## user

做成真实可调用的 CLI runner

## assistant

已做成“真实可调用”的 CLI runner。

新增/修改文件
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/src/cli/task-runner.ts
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/src/index.ts
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/package.json
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/tests/control-plane.test.ts

这次做了什么

1. 新增真实 CLI 入口
新文件：
- src/cli/task-runner.ts

提供两块核心能力：
- parseCliArgs(argv)
- runCliTask(args, config)

2. 支持真正的 --run 模式
现在 index.ts 支持三种模式：
- 默认：boot，打印配置
- --demo：跑安全 demo
- --run：跑真实 task runner

3. CLI 参数支持
当前支持这些关键参数：

必填
- --run
- --task-id <id>
- --repo-path <path>
- --image <docker-image>

可选
- --task-type <feature|bugfix|refactor|research|triage>
- --risk <low|medium|high>
- --retry-count <n>
- --base-commit <ref>
- --worktree-root <path>
- --memory <e.g. 2g>
- --cpus <e.g. 2>
- --pids-limit <n>
- --setup-timeout-seconds <n>
- --boot-timeout-seconds <n>
- --graceful-stop-seconds <n>
- --budget-limit-usd <n>
- --soft-limit-ratio <n>
- --hard-limit-ratio <n>
- --network-disabled
- --dry-run

命令透传
- 用 `--` 后面的内容当成容器内执行命令
例如：
  -- npm test

4. 真实运行逻辑
非 dry-run 时：
- 创建真实 WorktreeManager
- 创建真实 ContainerManager
- 创建 BudgetInterceptor
- 走 TaskRuntimeOrchestrator
- 若传了 `-- <cmd...>`，会在容器启动后执行 docker exec
- 最后仍由 orchestrator finally 清理：
  - stop container
  - remove container
  - cleanup worktree

5. dry-run 逻辑
带 --dry-run 时：
- 不碰真实 git
- 不碰真实 docker
- 仍完整走 orchestration 形状
- 返回一份结构化 summary
- 很适合先验参数、看路由和资源配置是否符合预期

6. 新增 npm 脚本
- npm run run-task

验证结果

我已经实际执行并通过：
- npm test
- npm run build
- npm run typecheck
- npm run demo
- npm run run-task -- --task-id cli-smoke --repo-path /tmp/repo --image node:22 --task-type feature --risk low --dry-run --network-disabled -- echo hello

结果：
- 测试 11/11 通过
- build 通过
- typecheck 通过
- demo 输出正常
- dry-run CLI 输出正常

你现在可以怎么用

1. 安全预演
在：
/home/siyuah/workspace/hermes_gpt54_starter/control-plane

执行：
npm run run-task -- \
  --task-id my-task \
  --repo-path /path/to/repo \
  --image node:22 \
  --task-type feature \
  --risk low \
  --dry-run \
  --network-disabled \
  -- npm test

2. 真实执行
注意：这会真的碰 git worktree 和 docker。

执行：
npm run run-task -- \
  --task-id my-task \
  --repo-path /path/to/repo \
  --image node:22 \
  --task-type feature \
  --risk low \
  --network-disabled \
  -- npm test

当前默认值
如果你不传，会默认：
- task-type: feature
- risk: low
- retry-count: 0
- memory: 2g
- cpus: 2
- pids-limit: 128
- setup-timeout-seconds: 60
- boot-timeout-seconds: 60
- graceful-stop-seconds: 10
- budget-limit-usd: 10
- soft-limit-ratio: 0.8
- hard-limit-ratio: 1
- worktree-root: <repo父目录>/.dark-factory/worktrees

这轮还补了哪些测试
新增验证：
- CLI 参数解析
- `--` 后命令透传
- dry-run 模式返回结构化结果，不触发真实 git/docker 路径

我建议你下一步先做这个
先拿你的真实仓库跑一遍 dry-run：
npm run run-task -- \
  --task-id smoke-1 \
  --repo-path <你的仓库路径> \
  --image node:22 \
  --dry-run \
  -- npm test

如果你愿意，我下一步可以继续帮你：
- 给这个 CLI runner 加 `--print-plan` / `--json` / `--help`
或者
- 直接替你拿一个真实 repo 跑一次 dry-run / 真跑。

## user

先加 --print-plan / --json / --help

## assistant

已加上：
- --print-plan
- --json
- --help

修改文件
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/src/cli/task-runner.ts
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/src/index.ts
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/tests/control-plane.test.ts

现在支持的行为

1. --help
用法：
- npm run run-task -- --help

效果：
- 打印 CLI 帮助
- 包含 mode、输出控制、run 参数、`-- <cmd...>` 说明

2. --print-plan
用法：
- npm run run-task -- --task-id t1 --repo-path /repo --image node:22 --dry-run --print-plan -- npm test

效果：
- 不执行任务
- 不创建 worktree/container
- 只输出“解析后的执行计划”
- 包含：
  - task
  - assignment
  - execution
    - repoPath
    - worktreeRoot
    - image
    - command
    - dryRun
    - networkDisabled
    - resources
    - budget

3. --json
可与 help / print-plan / run 组合
效果：
- 只输出 JSON
- 不加人类可读标题前缀

例如：
- npm run run-task -- --help --json
- npm run run-task -- --task-id t1 --repo-path /repo --image node:22 --print-plan --json
- npm run run-task -- --task-id t1 --repo-path /repo --image node:22 --dry-run --json -- echo hello

这轮新增的导出能力
在 task-runner.ts 里新增：
- buildRunPlan(args, config)
- renderHelp()

这样 index.ts 可以根据模式决定：
- 打 help
- 打 plan
- 真跑 task

本地验证结果

已执行并通过：
- npm test
- npm run build
- npm run typecheck
- npm run run-task -- --help
- npm run run-task -- --task-id plan-smoke --repo-path /tmp/repo --image node:22 --task-type feature --risk low --dry-run --print-plan --json -- echo hello

结果：
- tests: 14/14 通过
- build: 通过
- typecheck: 通过
- help: 正常输出
- print-plan + json: 正常输出 JSON

你现在可以直接这样用

查看帮助
- npm run run-task -- --help

看计划，不执行
- npm run run-task -- \
  --task-id smoke-1 \
  --repo-path /path/to/repo \
  --image node:22 \
  --print-plan

看 JSON 计划
- npm run run-task -- \
  --task-id smoke-1 \
  --repo-path /path/to/repo \
  --image node:22 \
  --print-plan \
  --json \
  -- npm test

dry-run JSON
- npm run run-task -- \
  --task-id smoke-1 \
  --repo-path /path/to/repo \
  --image node:22 \
  --dry-run \
  --json \
  -- npm test

小提示
你刚才 smoke 里没有加 --network-disabled，所以 print-plan 输出里是：
- "networkDisabled": false

如果你想禁网，要显式加：
- --network-disabled

如果你愿意，我下一步可以继续补：
- --output <pretty|json>
- --save-plan <file>
- --env KEY=VALUE 多次传入
- --task-type / --risk 的更完整校验错误提示

或者我也可以直接帮你拿某个真实 repo 跑一次 --print-plan。

## user

先补这些参数：

--output <pretty|json>
--save-plan <file>
--env KEY=VALUE（支持多次传入）
--task-type / --risk 的完整校验和错误提示

## assistant

已补上这 4 组参数能力：

- --output <pretty|json>
- --save-plan <file>
- --env KEY=VALUE
- --task-type / --risk 的完整校验和错误提示

修改文件
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/src/cli/task-runner.ts
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/src/index.ts
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/tests/control-plane.test.ts

这次具体补了什么

1. --output <pretty|json>
现在 CLI 输出模式正式支持：
- --output pretty
- --output json

兼容逻辑：
- --json 仍然可用，作为 --output json 的别名
- 如果两者都没传，默认 pretty

适用范围：
- --help
- --demo
- --run --print-plan
- --run 实际执行/--dry-run

2. --save-plan <file>
现在在：
- --run --print-plan

场景下可用：
- 会把 plan JSON 写到指定文件
- 同时仍输出到 stdout

我已经实际验证过：
- /tmp/hermes-plan.json 被成功写出
- 文件是有效 JSON

3. --env KEY=VALUE，支持多次传入
现在可重复写：
- --env NODE_ENV=test
- --env DEBUG=1

解析后会变成：
- environment: { NODE_ENV: "test", DEBUG: "1" }

并且会被带到：
- buildRunPlan() 的 execution.environment
- runCliTask() 的 runtime.environment
- toContainerInput() 的 environment
- 也就是未来真实容器执行时会传进 container manager

额外做了校验：
- 必须是 KEY=VALUE 格式
- key 必须是合法 shell 风格标识符
  - 例如：FOO、BAR_1 合法
  - 例如：1FOO、A-B 非法

4. --task-type / --risk 的完整校验和错误提示
现在错误信息更完整了：

无效 task type：
- Invalid --task-type: oops. Allowed: feature, bugfix, refactor, research, triage

无效 risk：
- Invalid --risk: oops. Allowed: low, medium, high

另外还顺手补了：
- --output 的错误提示
  - Invalid --output: xxx. Allowed: pretty, json
- --output 缺值提示
- --env 格式错误提示

5. plan/result 结构补充
plan 现在新增：
- savePlanPath
- execution.environment

run result 现在新增：
- runtime.environment

help 文本也更新了，明确写出：
- --output <pretty|json>
- --save-plan <file>
- --env KEY=VALUE

本地验证结果

已执行并通过：
- npm test
- npm run build
- npm run typecheck

以及 smoke command：
- npm run run-task -- --task-id env-smoke --repo-path /tmp/repo --image node:22 --task-type feature --risk low --dry-run --print-plan --output json --save-plan /tmp/hermes-plan.json --env NODE_ENV=test --env DEBUG=1 -- echo hello

验证结果：
- tests: 16/16 通过
- build: 通过
- typecheck: 通过
- plan 文件成功写出
- plan 文件 JSON 可解析
- plan 输出里包含 environment 和 savePlanPath

你现在可以这样用

1. pretty 输出
npm run run-task -- \
  --task-id t1 \
  --repo-path /path/to/repo \
  --image node:22 \
  --print-plan \
  --output pretty \
  --env NODE_ENV=test \
  --env DEBUG=1 \
  -- npm test

2. json 输出
npm run run-task -- \
  --task-id t1 \
  --repo-path /path/to/repo \
  --image node:22 \
  --print-plan \
  --output json \
  --env NODE_ENV=test \
  --env DEBUG=1 \
  -- npm test

3. 保存 plan
npm run run-task -- \
  --task-id t1 \
  --repo-path /path/to/repo \
  --image node:22 \
  --print-plan \
  --save-plan /tmp/task-plan.json \
  --output json \
  --env NODE_ENV=test \
  -- npm test

4. 错误示例
如果你传：
- --task-type abc

会得到类似：
- Invalid --task-type: abc. Allowed: feature, bugfix, refactor, research, triage

如果你愿意，我下一步建议补两个很实用的小东西：
- --validate-only
  只做参数与配置校验，不输出完整 plan
- --env-file <file>
  从 .env 风格文件批量读环境变量

或者我也可以直接帮你拿一个真实 repo 跑一次 print-plan。

## user

先补这两个：

--validate-only
--env-file <file>

## assistant

已补上这两个：

- --validate-only
- --env-file <file>

并且我把 validate-only 场景下的 env-file 合并结果也打通了。

修改文件
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/src/cli/task-runner.ts
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/src/index.ts
- /home/siyuah/workspace/hermes_gpt54_starter/control-plane/tests/control-plane.test.ts

这次新增的行为

1. --validate-only
用途：
- 只做参数/配置/路由解析与计划生成
- 不执行任务
- 不起 container
- 不碰 git worktree

行为上：
- 会输出 plan 结构
- 可与 --output / --json / --save-plan 组合
- 本质上是“比 --print-plan 更偏校验入口”的模式

当前 index.ts 逻辑是：
- --run + --print-plan => 输出 plan
- --run + --validate-only => 也输出 plan
- 两者都会走保存 plan 的逻辑（如果你传了 --save-plan）

2. --env-file <file>
用途：
- 从 env 文件读取 KEY=VALUE
- 支持多次传入
- 之后再用 inline `--env KEY=VALUE` 覆盖

优先级：
- env-file 先加载
- inline --env 后覆盖

例如：
--env-file a.env --env DEBUG=1

如果 a.env 里有：
DEBUG=0
NODE_ENV=prod

最后结果会是：
- DEBUG=1
- NODE_ENV=prod

新增的内部能力

1. 解析层
ParsedRunArgs 现在新增：
- validateOnly: boolean
- envFiles: string[]

2. 计划结构
CliRunPlan.execution 现在新增：
- envFiles: string[]

3. 计划解析能力
新增：
- resolveRunPlan(args, config)

它会：
- 读取 env-files
- 合并 inline env
- 生成“已解析环境变量”的最终 plan

这个能力现在被 index.ts 用在：
- --print-plan
- --validate-only

所以 plan 输出里看到的是“最终生效环境”。

4. 环境加载器
新增内部逻辑：
- loadEnvironment(envFiles, inlineEnvironment)

支持：
- 空行跳过
- `#` 注释跳过
- KEY=VALUE 解析
- key 合法性校验
- 多文件顺序合并
- inline 覆盖 file

本地验证结果

已执行并通过：
- npm test
- npm run build
- npm run typecheck

以及 smoke command：
- npm run run-task -- --task-id validate-smoke --repo-path /tmp/repo --image node:22 --task-type feature --risk low --validate-only --output json --env-file /tmp/hermes-extra.env --env DEBUG=1 --save-plan /tmp/hermes-validate-plan.json -- echo hello

验证结果：
- tests: 18/18 通过
- build: 通过
- typecheck: 通过
- validate-only 输出正常
- save-plan 文件成功写出
- plan 文件里 environment 已正确合并为：
  - NODE_ENV=fromfile
  - DEBUG=1
- envFiles 也正确保留在输出中

你现在可以这样用

1. 只校验，不执行
npm run run-task -- \
  --task-id t1 \
  --repo-path /path/to/repo \
  --image node:22 \
  --validate-only

2. 用 env-file + inline 覆盖
假设 /tmp/app.env 内容：
NODE_ENV=production
DEBUG=0

执行：
npm run run-task -- \
  --task-id t1 \
  --repo-path /path/to/repo \
  --image node:22 \
  --validate-only \
  --output json \
  --env-file /tmp/app.env \
  --env DEBUG=1 \
  -- echo hello

输出里最终 environment 会是：
- NODE_ENV=production
- DEBUG=1

3. 校验并保存计划
npm run run-task -- \
  --task-id t1 \
  --repo-path /path/to/repo \
  --image node:22 \
  --validate-only \
  --save-plan /tmp/task-plan.json \
  --env-file /tmp/app.env \
  --env DEBUG=1

补充说明
- --validate-only 现在输出的是 plan 结构，不是 run 结果
- --print-plan 和 --validate-only 现在很接近
  - 前者偏“展示执行计划”
  - 后者偏“只校验，不执行”
- 目前两者都复用同一套 plan 输出结构

如果你愿意，我下一步建议做一个小收口：
- 把 --print-plan 和 --validate-only 的差异语义再明确一点
比如：
  - --validate-only 额外检查 repoPath 是否存在、image 是否可用格式正确
  - --print-plan 只是纯计划展示

要不要我继续把这层“前置校验”也补上？
